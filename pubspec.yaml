# Green Market - Eco-friendly Marketplace App
# สำหรับ Flutter version 3.32.4 และ Dart SDK 3.8.1
name: green_market
description: "Green Market - แอปพลิเคชันตลาดออนไลน์เพื่อสิ่งแวดล้อม สำหรับการซื้อขายสินค้าเป็นมิตรกับธรรมชาติ"

publish_to: 'none' # ไม่เผยแพร่ไปยัง pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # === Core Flutter Dependencies ===
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # === Firebase Backend Services ===
  # ใช้เวอร์ชันล่าสุดที่รองรับ Flutter 3.32.4 และ Dart SDK ใหม่
  firebase_core: ^4.0.0           # ล่าสุดที่รองรับ Dart SDK ใหม่
  firebase_auth: ^6.0.0           # ล่าสุดที่รองรับ Dart SDK ใหม่  
  cloud_firestore: ^6.0.0        # ล่าสุดที่รองรับ Dart SDK ใหม่
  firebase_storage: ^13.0.0       # ล่าสุดที่รองรับ Dart SDK ใหม่
  firebase_messaging: ^16.0.0     # ล่าสุดที่รองรับ Dart SDK ใหม่

  # === State Management & Architecture ===
  provider: ^6.1.2                # State management pattern

  # === HTTP & Network ===
  http: ^1.2.2                    # HTTP client for network requests

  # === UI & User Experience ===
  flutter_colorpicker: ^1.1.0     # Color picker widget
  fl_chart: ^0.69.0               # Charts and graphs (downgrade for compatibility)
  flutter_markdown: ^0.7.7+1      # Markdown rendering (keep using until replacement available)
  flutter_rating_bar: ^4.0.1      # Product rating stars
  photo_view: ^0.15.0             # Image viewer with zoom
  carousel_slider: ^5.0.0         # Image carousels
  shimmer: ^3.0.0                 # Loading animations
  cached_network_image: ^3.4.1    # Updated - Image caching
  video_player: ^2.9.2            # Video playback for community posts

  # === Device & Platform Integration ===
  image_picker: ^1.1.2            # Camera & gallery access
  file_picker: ^10.2.0            # File system access
  url_launcher: ^6.3.0            # Open URLs and apps
  share_plus: ^12.0.1             # Native sharing functionality
  google_sign_in: ^7.1.1          # Google authentication
  flutter_local_notifications: ^19.4.0  # Push notifications
  connectivity_plus: ^7.0.0       # Network connectivity status
  device_info_plus: ^12.2.0       # Device information
  package_info_plus: ^9.0.0       # App version and package info
  shared_preferences: ^2.2.2      # Local storage
  path_provider: ^2.1.4           # File paths
  geolocator: ^13.0.2             # NEW - GPS location services
  geocoding: ^3.0.0               # NEW - Convert coordinates to addresses

  # === Data & Utilities ===
  intl: ^0.20.2                   # Internationalization
  timeago: ^3.3.0                 # Updated - Time formatting
  uuid: ^4.4.0                    # Unique ID generation
  crypto: ^3.0.3                  # Encryption utilities
  collection: ^1.18.0             # Collection utilities
  string_similarity: ^2.0.0       # String comparison
  qr_flutter: ^4.1.0              # QR code generation
  image: ^4.2.0                   # Image processing and compression
  flutter_image_compress: ^2.3.0  # Image compression (NEW - for storage optimization)
  path: ^1.9.0                    # Path manipulation utilities (NEW)
  
  # === Reactive Programming ===
  rxdart: ^0.28.0                 # Reactive extensions (updated for compatibility)
  logger: ^2.3.0                  # Logging utilities
  
  # === Live Streaming ===
  agora_rtc_engine: ^6.3.2        # Live streaming SDK
  permission_handler: ^11.0.1     # Camera/Mic permissions
  wakelock_plus: ^1.2.8           # Keep screen on during live
dev_dependencies:
  # === Testing Framework ===
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0           # Code analysis rules
  
  # === Testing Utilities ===
  mockito: ^5.4.4                 # Mock objects for testing
  build_runner: ^2.4.9            # Code generation
  fake_cloud_firestore: ^4.0.0    # Firebase mocking for tests (compatible version)

flutter:
  uses-material-design: true
  
  # === Assets Configuration ===
  assets:
    - assets/logo.jpg
    - assets/

  # === Custom Fonts ===
  fonts:
    - family: Sarabun
      fonts:
        - asset: assets/fonts/Sarabun-Regular.ttf
          weight: 400
        - asset: assets/fonts/Sarabun-Bold.ttf
          weight: 700

# === เอกสารสำคัญ ===
# 📋 การบำรุงรักษา:
#   - ตรวจสอบ dependencies ทุก 3 เดือน
#   - รัน `flutter pub outdated` เพื่อดู updates
#   - ทดสอบใน platforms ทั้งหมดหลัง update
#
# 🚀 Performance Tips:
#   - ใช้ cached_network_image สำหรับรูปภาพ
#   - ใช้ shimmer สำหรับ loading states
#   - ใช้ connectivity_plus ตรวจสอบ network
#
# 🔐 Security:
#   - อัพเดต Firebase packages เป็นประจำ
#   - ใช้ crypto package สำหรับ hashing
#   - ตรวจสอบ permissions ใน Android/iOS
#
# 📱 Platform Support:
#   - Android: API 21+ (Android 5.0+)
#   - iOS: iOS 12.0+
#   - Web: Modern browsers
#   - Windows: Windows 10+
