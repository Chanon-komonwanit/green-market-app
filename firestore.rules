rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ชั่วคราวเปิดให้อ่านทุกอย่างได้เพื่อแก้ปัญหา
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}